#include "main.h"
#include <string.h>
/**
 * _strlen - Function to return length of a string
 * @s: Pointer to a string
 * Return: Always 0
 */

int _strlen(char *s)
{
	int a = strlen(s);

	return (a);
}
