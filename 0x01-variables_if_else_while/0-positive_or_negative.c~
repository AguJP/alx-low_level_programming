#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/* more headers goes there */

/* main - Entry point */
int main(void)
{
  int n;

  srand(time(0));
  n = rand() - RAND_MAX / 2;

  return (0);
}
