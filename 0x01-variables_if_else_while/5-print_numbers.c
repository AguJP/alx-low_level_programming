#include <stdio.h>
/**
 * main - Entry point, Prints all single digits of base 10
 * Return: Always 0
 */

int main(void)
{
int m;
for (m = 0; m <= 9 ; m++)
{
printf("%d", m);
}
printf("\n");
return (0);
}
