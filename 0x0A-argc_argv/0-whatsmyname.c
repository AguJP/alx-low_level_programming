#include <stdio.h>

/**
 * main - program to print the name of a program
 * @argc: argument count
 * @argv: argument vector
 * Return: Always 0
 */

int main (int argc, char *argv[])
{
	printf (argv[0]"\n");
}
